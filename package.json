{
  "name": "skills-registry-catalog",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "catalog": "node scripts/catalog.mjs --bootstrap",
    "validate": "node scripts/validate.mjs"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  }
}
